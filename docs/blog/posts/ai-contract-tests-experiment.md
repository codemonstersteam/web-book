---  
title: Со-творчество с машиной 2 > контрактные тесты
date: 2025-08-07 
authors:  
  - gardener  
slug: ai-contract-tests-example-go  
description:   
    Со-творчество с машиной
    Пишем утилиту валидации контрактов по спецификации AsyncAPI 3.0 на Go
categories:  
  - code
  - claude
  - contract-tests
  - tests
  - ai
  - go
  - devops
---
# Эксперимент с ИИ-ассистентом: бег от Пакт в сторону валидации спецификаций
![ai-contract-tests-experiment.png](ai-contract-tests-experiment.png)

Перерыв в кодописи только идет мне на пользу.  

### Что было сделано
За несколько недель разработки с ИИ-ассистентом создан полнофункциональный инструмент валидации контрактов:

- **Модуль парсера AsyncAPI 3.0** — полная поддержка спецификации
- **Модуль валидации каналов** — проверка совместимости по протоколам и структуре сообщений  
- **Contract Validator с ROP** — устойчивая обработка ошибок по Railway-oriented Programming
- **300+ comprehensive unit-тестов** — покрытие всех функций с edge cases
- **Стандартизация ошибок** — информативные диагностические сообщения для production

Ключевые выводы, которые я сделал:
Если ты умеешь проектировать, раскладывать софт на модули, то можно создавать качественное решение с ИИ-ассистентом.

Нужно грамотно проектировать архитектуру, декомпозировать задачи,
упрощать контекст как себе так и машине.  
Так обычно стоит и работать с людьми. 
У тебя в руках очень производительный и мощный инструмент, чтобы управиться с этим кибер-имплантом нужно понимать как создавать код, который легко проверить и осознать. 

<!-- more -->

## Мой подход к разработке

Мы лаконично описали нашу разработку с использованием конвейера непрерывной поставки в handbook. 
Получается кратко и утилитарно. 

Как супергерой надевает костюм в облипку и спускается в темные улицы колечить злодеев, так я днем менеджер, а ранним утром - разработчик экспериментатор в труселях и тапках. На стиле.
Экспериментатор дорабатывает учебный сервис по стандарту — странно продвигать стандарт, которым не пользуешься, да и дело довести нужно до конца. 
Очень помогает пройтись по пути разработки свежим взглядом менеджера и тупицы, попробовать все шаги своими руками.  

### Процесс разработки:

Зачем я это пишу здесь? Если бы я не видел всей картины, то не смог бы сгенерить идею.  

Процесс разработки кратко можно описать так.
1. **Спецификация** — пишем AsyncAPI 3.0
2. **Документация** — описываем в README.md задачу компонента и способ решения  
3. **Компонентные тесты** — тесты по описанию
4. **Реализация** — опираемся на понятно описанные постусловия
5. **Архитектура** — отделяем бизнес-логику от операций ввода-вывода
6. **Простота** — избегаем излишне сложных слов, упрощаем на каждом этапе

Я доработал [сервис](https://git.codemonsters.team/guides/mq-rest-sync-adapter) перевел бизнес-логику на простую структуру, отделив от ввода-вывода. 
Добавил специй в виде R.O.P. (Railway-oriented Programming).

Реализовал [сервис-поставщик](https://git.codemonsters.team/guides/mq-rest-sync-adapter) совместно с машиной на Go.

И вот пришла пора написать [контрактные тесты потребителя](https://git.codemonsters.team/guides/mq-rest-sync-adapter/contract-tests).

## Исследование инструментов контрактного тестирования

Исследовал еще раз киберпространство на предмет инструментов для проверки контрактов — свежим взглядом посмотрел на Pact, Spring Cloud Contract, посмотрел на эту штуковину https://www.cncf.io/projects/microcks/
https://microcks.io/.

Свежим взглядом **Pact** — вроде норм, не зависит от Spring Framework. Уже хорошо. Spring программисты мне невыносимы.

Честно Перечитал лучшие практики Pact, рекомендации. Все красиво. Начал описывать в BDD стиле тесты в этом сервисе и понял — **не могу**. Пальцы отказываются. Меня осенило.

### Момент озарения

Зачем мне писать дополнительный код в BDD стиле, когда у меня есть спецификация AsyncAPI 3.0 у поставщика и потребителя? Я все уже написал в репе.  

У меня есть:
- Сервис-потребитель 
- Сервис-поставщик
- AsyncAPI 3.0 спецификации для обоих
- компонентные тесты 

Зачем мне еще что-то писать? 

И вот моя гепотиза, с которой я живу последнее время.  

**Могу ли я проверить соответствие контрактов, опираясь на эти спецификации?**

Логично, что **да**. Будем смотреть на случай актуальных спецификаций и соответствие сервисов их спецификациям.
Случай инженерной тупости и бутофории в виде неактуальных спецификации AsyncAPI 3.0 - не самый важный вопрос, хоть его и стоит поднять позже.  

### Решение

Я создал [утилиту на Go](https://git.codemonsters.team/guides/mq-rest-sync-adapter/-/tree/feature/contract-tests/contract-tests?ref_type=heads), которая сверяет спецификации потребителя и поставщика из Git. И это хорошо работает в CI.

Я начал увлеченно экспериментировать над качеством разработки c Claude. 

Задавать вопросы самому себе, опираясь но новый опыт:  

> Как я могу разработать софт лучше, читабельнее, качественнее — с тестами и при этом не написать ни строчки кода вручную?

## Момент истины: 11.07.2025, 5:00
[вторая версия утилиты](https://git.codemonsters.team/guides/mq-rest-sync-adapter/-/tree/feature/contract-tests/contract-tests-v2?ref_type=heads)  

Еле встал в 5 утра. Добрался до Claude терминала, продирая глаза. В тот день стояла дикая жара в Подмосковье. 

> Максим, сейчас ты оператор клод-терминала.

Решил изменить подход к разработке утилиты проверки контрактов по AsyncAPI потребителя и поставщика.

### TDD подход с Claude

Подумал: давай проверю TDD подход и пойду маленькими шажками.

**Гипотеза для проверки:** канал и протокол потребителя поддерживается поставщиком.

**Предохранители:** 
- Канал должен содержать обязательно ссылку на сервер
- Описание сервера обязательно содержит протокол

От тестов создал с Claude первый шаг проверки. Сохранял каждый шаг TDD в Git.

**Результат:** Получилось лаконично и уже работает!

Я вдохновлен — круто. 
Человек-читатель, мне стало еще интереснее работать с машиной. Продуктивно.

Единственное, что я написал тогда сам — одну корректировку в спецификацию. Остальные корректировки вписывал курсором.

### Ключевые выводы

**Вывод №1:** Подход от тестов маленькими инкрементами позволяет сохранять контекст и делать качественно совместно с машиной.

**План развития:** Опираясь на тест канала взаимодействия, начну от тестов проверять соответствие структуры сообщений (запрос, ответ) в канале.

С тех пор прошло несколько дней, и я еще больше улучшил взаимодействие с ИИ-ассистентом. В своих экспериментах я ушел намного дальше.

### Итоговый вывод

Если ты умеешь проектировать, раскладывать софт на модули, то можно работать и выдавать качественное решение.
Маленькими инкрементами с тестами можно работать прямо по TDD подходу:

1. **Проектируешь** по модулям проект  
2. **Задаешь** формат интеграций модулей
3. **Выдаешь** четкие задачи ИИ, уменьшая контекст как себе, так и машине


---

## Техническое задание v2

**Цель:** Написать инструмент валидации контрактных тестов на Go по спецификации AsyncAPI 3.0 потребителя и поставщика.

Процесс и результаты разработки: 
[вторая версия утилиты](https://git.codemonsters.team/guides/mq-rest-sync-adapter/-/tree/feature/contract-tests/contract-tests-v2?ref_type=heads)  


**Методология:** TDD (Test-Driven Development) от тестов.

### Входные данные

**Конфигурационный файл** `contract-tests.yaml`:
- Путь/URL к спецификации потребителя (consumer)
- Канал взаимодействия с поставщиком
- URL к спецификации поставщика



---

## Архитектурные решения

### Шаг 1: Модуль парсера

Для улучшения проекта нужен модуль, который собирает из спецификации структуру, с которой удобно работать:
- **Вход:** строковое представление AsyncAPI 3.0
- **Выход:** структура данных для модуля валидации каналов

### Шаг 2: Contract Validator

Модуль `contract_validator` проектируется по **ROP** ([Railway-oriented Programming](https://fsharpforfunandprofit.com/rop/)), который поддерживается Go из коробки.

#### Основная функция `validate` включает шаги:

1. **Извлечение конфигурации** из `contract-tests.yaml`
   - При ошибке → возврат исчерпывающей информации
2. **Парсинг спецификации потребителя** в структуру
   - При ошибке → возврат исчерпывающей информации
3. **Парсинг спецификации поставщика** в структуру
   - При ошибке → возврат исчерпывающей информации
4. **Формирование структуры** `ContractValidate`:
   - Название канала потребителя
   - Структура спецификации поставщика
   - Структура спецификации потребителя
5. **Передача в модуль валидации каналов**
   - При ошибке → возврат исчерпывающей информации

Необходимо упростить модуль валидации каналов и адаптировать его для работы с новой структурой.


### Шаг 3: Модуль валидации каналов

#### Входная структура:
```go
type ContractValidate struct {
    ConsumerChannelName string
    ProviderSpec        *parser.AsyncAPISpec
    ConsumerSpec        *parser.AsyncAPISpec
}
```

#### Задача модуля
Написать модуль валидации каналов, где каждый канал AsyncAPI ссылается на сервер с указанным протоколом взаимодействия.

#### Выходная структура
Необходимо по структурам спецификаций собрать новую выходную структуру:

**Канал потребителя:**
- Имя канала
- Протокол
- Исходящее сообщение (out) — структура сообщений с типами
- Ожидаемый ответ (in) — структура сообщений с типами

**Канал поставщика** (соответствует каналу потребителя по протоколу):
- Имя канала
- Протокол  
- Входящее сообщение (in) — структура сообщений с типами
- Ответное сообщение (out) — структура сообщений с типами

Функция преобразует `ContractValidate` в новую, более полную структуру.

#### Особенности реализации

**Извлечение сообщений:**
- Входящие сообщения описаны в `channels` (канал ссылается на сервер) и `operations` (операция ссылается на канал)
- В `operations` должен быть `reply` — указывает на исходящее сообщение операции

**Поиск соответствующего канала:**
У поставщика может быть несколько каналов с одинаковым протоколом, но разными структурами сообщений. Поиск нужного канала поставщика происходит по соответствию структуры сообщений потребителя.

#### Тестирование
- **1 юнит-тест:** структуру можно собрать по входным данным из конфигурации
- **Универсальность:** код должен работать только с спецификацией AsyncAPI
- **Тестовые данные:** хранить в `testdata/channel_validator`


---

## Дополнительные требования (19.07.25)

Добавить тест на случай, когда у поставщика есть несколько подходящих каналов по протоколу сервера, и лишь один соответствует по структуре сообщений. 

**Цель тестирования:** показать, как документация демонстрирует соответствие конкретного канала потребителя каналу поставщика.

---

## Результат эксперимента

### Что было сделано
За несколько недель разработки с ИИ-ассистентом создан полнофункциональный инструмент валидации контрактов:

- **Модуль парсера AsyncAPI 3.0** — полная поддержка спецификации
- **Модуль валидации каналов** — проверка совместимости по протоколам и структуре сообщений  
- **Contract Validator с ROP** — устойчивая обработка ошибок по Railway-oriented Programming
- **300+ comprehensive unit-тестов** — покрытие всех функций с edge cases
- **Стандартизация ошибок** — информативные диагностические сообщения для production

### Ключевые выводы

**1. TDD с ИИ работает**
- Маленькие инкременты позволяют сохранять контекст
- От тестов к реализации = качественный код без переписывания
- ИИ хорошо следует техническому заданию при четких требованиях

**2. Comprehensive тестирование критично**
- Обнаружено и исправлено **2 критических бага** в процессе тестирования
- AsyncAPI 3.0 compliance требует покрытия множества edge cases
- Unit-тесты предотвращают регрессии при рефакторинге

**3. Качество архитектуры**
- ROP подход обеспечивает надежную обработку ошибок
- Стандартизированные сообщения упрощают диагностику
- Модульная структура позволяет легко расширять функциональность

**Время разработки:** ~10-12 часов активной работы с ИИ  
**Результат:** Production-ready инструмент с полным тестовым покрытием

*Статья написана в процессе разработки инструмента валидации контрактов по спецификациям AsyncAPI 3.0. Описанный подход TDD с использованием ИИ-ассистента показал высокую эффективность для создания качественного, хорошо протестированного кода.*

[Вся история моей с машиной работы](https://git.codemonsters.team/guides/mq-rest-sync-adapter/-/blob/feature/contract-tests/contract-tests-v2/STORY.md?ref_type=heads)
