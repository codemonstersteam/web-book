# .githooks

Git-хуки для проекта. После клонирования репозитория выполни один раз:

```bash
git config core.hooksPath .githooks
```

## Хуки

| Хук | Что делает |
|-----|-----------|
| `pre-commit` | Запускает `mkdocs build` — блокирует коммит если сборка падает с ошибкой |
